class Helpers {
  static interpolate(start, end, stepCount, playerId, direction) {
    const stepSize = (end - start) / stepCount;

    let timeOutId;
    let result = start;

    console.log(
      "interpolate",
      playerId,
      "start:",
      start,
      "end:",
      end,
      direction
    );
    
    
  }
}
